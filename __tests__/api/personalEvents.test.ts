// Personal Events API tests are temporarily disabled
// The PersonalEvent models are not yet available in the production Prisma schema
// These tests will be re-enabled once the personal events feature is fully implemented

describe.skip('/api/personal-events', () => {
  it('should be implemented when PersonalEvent models are added to production schema', () => {
    expect(true).toBe(true);
  });
});
